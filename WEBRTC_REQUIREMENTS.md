# WebRTC Phone 仕様要求書

## 1. 目的

本アプリは、スマートフォン通話アプリとして利用することを前提に、UI/UX と機能を整理した WebRTC 電話アプリである。

PC ブラウザで開いた場合でも一般的な Web ページとして見せるのではなく、スマホアプリに近い見た目と操作感に統一する。

本書は、WebRTC アプリの実装・改修・検証に必要な要求事項を整理するための仕様要求書である。

## 2. 基本方針

- 画面全体はスマホサイズ基準で設計する。
- PC ブラウザ表示時もスマホ幅相当の中央寄せレイアウトとする。
- iOS / Android のネイティブ通話アプリに近い UI を目指す。
- ボタン、カード、入力欄、余白、アイコンサイズは全画面で統一する。
- 文字だけで意味を伝えるのではなく、適切なアイコンを用いて視認性を上げる。
- 詳細表示は可能な限りポップアップではなく専用画面遷移で扱う。

## 3. UI / UX 共通要求

### 3.1 レイアウト

- 画面全体を PC 幅いっぱいに広げない。
- 最大幅はスマホ相当の 390px から 430px 程度に制限する。
- PC ブラウザでは中央寄せで表示する。
- Web 版でもスマホアプリと同じ余白、同じボタンサイズ、同じ情報密度にする。

### 3.2 デザイン統一

- ヘッダー、下部メニュー、カード、入力欄、ボタンの高さと角丸を統一する。
- アイコンサイズは画面ごとにばらつかせず、同じデザインルールに揃える。
- 不要に大きい余白を減らし、1 画面に表示できる情報量を増やす。
- ダークモードに対応する。

### 3.3 画面遷移・操作感

- ネイティブアプリに近い画面遷移にする。
- お気に入り追加、削除、再発信などの操作には軽いフィードバックを付ける。
- 戻る操作時は文脈に応じて元画面に戻る。

## 4. App Shell / 下部メニュー

- 下部メニューは常に画面下部へ固定表示する。
- スクロールしても一緒に動かないようにする。
- 下部メニューは App Shell の一部として全画面で同一位置に表示する。
- safe area を考慮し、コンテンツが下部メニューに隠れないようにする。
- z-index を適切に設定し、他要素の背面に回らないようにする。

### 4.1 表示要件

- 履歴
- キーパッド
- 連絡先
- お気に入り

### 4.2 挙動要件

- 現在タブは明確にアクティブ表示する。
- 押下時の視覚フィードバックを付ける。
- 未使用タブや将来用タブは露出しない。

## 5. ヘッダー

- 上部ヘッダーはできるだけ固定表示にする。
- 画面ごとに中身は変えてよいが、構造とサイズは統一する。
- 「キーパッド」「設定」などはテキストだけではなくアイコン化する。
- logout は常時露出させず、メニューや設定画面側に寄せる。

## 6. ダイヤル画面

- ダイヤル入力欄は読みやすいサイズにする。
- 長い SIP URI はそのまま大きく表示しない。
- ダイヤルボタンは現在の一般的な通話アプリに近いサイズにする。
- 数字ボタンと操作ボタンは統一デザインにする。
- 発信方法は設定可能とする。

### 6.1 発信方法

- SIP 発信
- 端末の通常電話番号への発信
- 店舗番号を使った発信

### 6.2 表示要件

- 最近の履歴はスクロールしなくても 1 から 3 件程度見えるようにする。
- 発信ボタンは視認しやすい主要ボタンとして扱う。

## 7. 通話中画面

- 通話中画面はキーパッドをデフォルトで表示しない。
- キーパッド押下時は現在画面の下に展開するのではなく、専用画面へ遷移する。
- キーパッド画面を閉じたら元の通話画面へ戻る。
- 通話相手が連絡先に登録されている場合は連絡先名を大きく表示する。
- 未登録の場合は電話番号または SIP URI を表示する。
- 長い SIP URI は省略表示する。

### 7.1 通話状態表示

- 「通話中」「保留中」「着信中」などの状態は見やすく整理する。
- 通話時間は一目で分かる位置に表示する。

### 7.2 操作ボタン

- ミュート
- 保留
- スピーカー
- 転送
- キーパッド

### 7.3 操作ボタンのレイアウト

- 縦一列ではなく、2 列または 3 列グリッドで表示する。
- ボタンサイズは大きすぎないように調整する。
- 余白を減らして情報密度を上げる。
- 「通話終了」ボタンは画面下部に固定する。

## 8. 着信画面

- 着信画面は着信者情報、着信状態、応答ボタン、拒否ボタンを中心に表示する。
- 応答ボタンは主ボタン、拒否ボタンは補助ボタンとして扱う。
- 着信中に連絡先名が分かる場合は名前を優先表示する。
- 未登録なら電話番号または SIP URI を表示する。

## 9. 通話履歴

- 通話履歴は一覧性を高く保つ。
- 「すべて」「着信」「発信」「不在着信」でフィルタできるようにする。
- 日付は「今日」「昨日」「YYYY/MM/DD」の形式で分かりやすく表示する。
- 通話時間も一覧に表示する。
- 各履歴には再発信ボタンと詳細遷移ボタンを配置する。

### 9.1 表示優先順位

- CTI 名やアドレス帳名が取得できる場合はそれを優先する。
- 次に電話番号
- 最後に SIP URI

### 9.2 詳細画面

- 履歴詳細はポップアップではなく専用画面で表示する。
- 種別、状態、日時、通話時間、店舗情報を表示する。

### 9.3 再発信

- 再発信アイコンは以前の電話アイコンに戻す。
- 分かりにくい独自アイコンは使わない。

## 10. 連絡先

- 一覧表示は「アバター」「名前」「補足情報」「発信ボタン」を 1 行にまとめる。
- 画像未設定時はデフォルトアバターを表示する。
- 新規追加時点ではお気に入りにしない。
- お気に入り追加時のみ星マークを表示する。

### 10.1 検索

- 名前
- 電話番号
- SIP
- メモ

### 10.2 並び順

- 五十音順 / アルファベット順
- よく使う順
- 最近更新順

### 10.3 よく使う連絡先

- callCount や利用頻度で上位表示できるようにする。
- 候補として出しやすい並びにする。

### 10.4 詳細画面

- 上部カードの余白は大きすぎないようにする。
- 「発信」「お気に入り」「編集」「削除」ボタンはコンパクトにする。
- 「名前」「番号 / SIP」「メモ」の間隔は詰める。

## 11. 設定画面

- ラベル、入力欄、説明文、余白のバランスを整える。
- 1 画面内で情報が過剰に間延びしないようにする。
- 設定画面も他画面と同じサイズルールに合わせる。
- 保存後は成功メッセージまたは状態変化が分かるようにする。

## 12. プロビジョニング

- プロビジョニングに対応する。
- 手動入力前提にはしない。
- kfPhone 的な運用を前提に、設定値を一括投入できるようにする。

### 12.1 対応対象

- SIP 設定
- WebSocket 設定
- 認証ユーザー
- パスワード
- 発信方式
- 店舗設定
- 必要に応じて CTI / 端末連携に関する設定

### 12.2 要求

- deep link または同等の外部起動方式で設定を投入できるようにする。
- 受け取った設定はアプリ内に保存できるようにする。
- 自動登録を行うかどうかも制御可能にする。
- kfPhone の仕様に近い運用を目指す。

## 13. ログ送信

- ログ送信はクリップボードやメール起動任せにしない。
- サポート利用時はワンタップで dev 宛に送れることを優先する。
- 既存アプリの `sendLog()` 相当の処理を参考にする。

### 13.1 API 要件

- `method=sendMail`
- `mail=admin2@knowledge-flow.net`
- `password=egwasaeVNCoFkut3`
- `to=dev.knowledgeflow@gmail.com`
- `subject=title`
- `text=part/total + header + text`
- `POST https://dental-apo.jp/ajax/api/sptest`

### 13.2 挙動要件

- 長いログは複数パートに分割して送信する。
- 送信失敗時はエラー理由が分かるようにする。
- サポート担当が再操作しやすいフィードバックを表示する。
- ログ表示では各エントリの前に改行を入れ、ログ時刻が必ず行頭に来るようにする。
- ログ送信はホーム画面の右上メニューから直接実行できるようにする。
- 右上メニューから実行した場合も既存の `sendLog()` / API 送信処理を共用し、別系統の送信実装を増やさない。

## 14. Register 更新時の挙動

- register のタイミングでメイン画面が一気に消えたり再構築されないようにする。
- 更新中も UI が不自然にちらつかないようにする。
- 状態変化は必要最小限の再描画で反映する。

## 15. Android 9 / Android 10 対応

- Android 9 でアプリを開いてしばらくすると落ちる問題に対応する。
- Android 10 でもしばらく動かすと落ちる問題に対応する。
- 長時間起動、通話待機、ログ送信、register 更新、プロビジョニングを含めて検証する。
- サポート現場で Android 9 端末を使う前提で品質を確保する。

### 15.1 確認観点

- 起動直後に落ちないか
- 数分から十数分放置で落ちないか
- 通話待受中に落ちないか
- ログ送信時に落ちないか
- WebView renderer crash 発生時に復旧できるか

## 16. 実機確認項目

- ログ送信が動くか
- 電話できるか
- 通話履歴から発信できるか
- プロビジョニングできるか
- register 更新時にちらつかないか
- Android 9 / Android 10 で落ちないか
- 店舗番号 / 通常番号の発信が正しいか
- 履歴・連絡先で名前優先表示が正しいか

## 17. フィードバック方針

- お気に入り追加
- 削除
- 保存
- ログ送信
- 発信
- 再発信

上記の各操作には、アニメーションやトースト等の軽いフィードバックを付ける。

## 18. 実装優先度

1. App Shell、ヘッダー、下部メニューの統一
2. 通話中画面
3. ダイヤル / 発信画面
4. 通話履歴
5. 連絡先
6. 設定画面
7. プロビジョニング
8. 店舗番号 / 通常番号対応
9. ログ送信
10. 実機確認とクラッシュ対策
11. Android 9 / Android 10 長時間安定化

## 19. 補足

- 本書は現時点の要求整理用ドキュメントであり、実装詳細は別資料に分離してよい。
- 今後、UI の統一ルール、サポート運用手順、プロビジョニング仕様、CTI 連携仕様は追加の詳細仕様書へ展開することを想定する。

## 20. AGI 着信連携仕様

本節は Google Docs の「AGIアプリ API仕様」と `C:\project\tool\agi-app` の実装を基に整理する。

### 20.1 ベース URL

- サーバー内部: `http://127.0.0.1:18088`
- 外部公開時: `https://test202606.mimio.jp/agi-api`

### 20.2 全体フロー

1. Asterisk が着信を受ける。
2. dialplan から FastAGI を呼び出す。
3. AGI が `callId` を発行する。
4. AGI が登録済み端末へ Push 通知を送信する。
5. 端末アプリが Push を受信して着信を表示する。
6. ユーザーが応答操作を行う。
7. 端末アプリが Asterisk へ SIP REGISTER する。
8. 端末アプリが AGI へ DIAL リクエストを送る。
9. AGI が対象端末へ `Dial(PJSIP/<sipEndpoint>)` を実行する。
10. 端末が応答し、通話を開始する。

着信時に AGI から直ちに DIAL せず、応答端末の REGISTER と DIAL リクエストを待ってから対象端末のみを呼び出す。

### 20.3 API

#### 生存確認

- `GET /health`
- 成功レスポンス: `{"ok":true}`

#### 端末登録

- `POST /devices/register`
- `Content-Type: application/json`
- リクエスト: `deviceId`, `pushToken`, `sipEndpoint`
- `deviceId` は端末管理 ID、`pushToken` は FCM token、`sipEndpoint` は Asterisk の PJSIP endpoint 名とする。
- 成功レスポンス: `{"ok":true}`

#### SIP REGISTER 完了通知

- `POST /devices/registered`
- リクエスト: `deviceId`
- SIP REGISTER 完了後に通知する。

#### DIAL リクエスト

- `POST /dial-request`
- `Content-Type: application/json`
- リクエスト: `callId`, `deviceId`, `sipEndpoint`
- `callId` は Push で受信した着信 ID とする。
- `sipEndpoint` 省略時は登録済み端末情報を使用する。
- 最初に受理された端末だけを有効とし、存在しない `callId`、待機時間超過、他端末応答済みの場合は失敗とする。

### 20.4 Push payload

AGI から FCM へ送信する data payload は次の値を含む。

- `type=incoming_call`
- `callId`
- `caller`
- `callee`
- `deviceId`

### 20.5 RINGEND WebSocket

- 接続 URL: `wss://test202606.mimio.jp/agi-ringend?deviceId={deviceId}`
- 端末は Push 受信後、着信を鳴らし始めるタイミングで接続する。
- 接続失敗時は 5 秒後に再試行する。
- 30 秒間接続できない場合は端末側で着信を終了する。
- 他端末の DIAL リクエスト受理時、AGI は応答端末以外へ `RINGEND` を送信する。
- メッセージは `type`, `callId`, `reason`, `answeredDeviceId` を含む。
- `reason` は `answered`, `registered`, `timeout` を想定する。
- 異なる `callId` の終了通知は現在の着信へ適用しない。

### 20.6 タイムアウトとログ

- DIAL リクエスト待機時間は `DIAL_REQUEST_TIMEOUT_MS` で設定する。
- 標準値は 30 秒、テスト時は 120 秒へ変更可能とする。
- サーバーログは `/var/log/agi-app.log` へ出力する。
- 正常系では incoming call、Push 送信、DIAL request accepted、dial start、call session closed を追跡できるようにする。

### 20.7 テスト環境

- 050 番号: `05058101566`
- SIP ドメイン: `test202606.mimio.jp`
- WebSocket URL: `wss://test202606.mimio.jp/webrtc/ws`
- 端末: `webrtc_101`, `webrtc_102`, `webrtc_103`
- password: `pass`
